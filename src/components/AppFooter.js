import React from 'react'
import { CFooter } from '@coreui/react'

const AppFooter = () => {
  return (
    <CFooter>
      <div>
      Admin dashboard *
      </div>
    </CFooter>
  )
}

export default React.memo(AppFooter)
